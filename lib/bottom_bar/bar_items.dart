import 'package:flutter/material.dart';

// ignore: camel_case_types
class bar_items extends StatelessWidget {
  const bar_items({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text('Bar items page'),
      ),
    );
  }
}
